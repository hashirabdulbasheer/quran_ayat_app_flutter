import 'package:flutter/material.dart';

class QuranCreateChallengeScreen extends StatelessWidget {
  const QuranCreateChallengeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(color: Colors.red);
  }
}
