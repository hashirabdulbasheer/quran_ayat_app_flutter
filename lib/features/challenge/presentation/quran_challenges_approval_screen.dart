import 'package:flutter/material.dart';

class QuranChallengesApprovalScreen extends StatelessWidget {
  const QuranChallengesApprovalScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
